diff --git a/node_modules/mineflayer/lib/plugins/bed.js b/node_modules/mineflayer/lib/plugins/bed.js
index 9b526f6..110d837 100644
--- a/node_modules/mineflayer/lib/plugins/bed.js
+++ b/node_modules/mineflayer/lib/plugins/bed.js
@@ -22,6 +22,28 @@ function inject (bot) {
       headOffset: new Vec3(0, 0, 1)
     }
 
+    const properties = bedBlock.getProperties?.()
+    if (properties?.part) {
+      metadata.part = properties.part === 'head'
+      metadata.occupied = properties.occupied === true || properties.occupied === 'true'
+
+      switch (properties.facing) {
+        case 'west':
+          metadata.facing = 1
+          metadata.headOffset.set(-1, 0, 0)
+          break
+        case 'north':
+          metadata.facing = 2
+          metadata.headOffset.set(0, 0, -1)
+          break
+        case 'east':
+          metadata.facing = 3
+          metadata.headOffset.set(1, 0, 0)
+          break
+      }
+      return metadata
+    }
+
     if (bot.supportFeature('blockStateId')) {
       const state = bedBlock.stateId - bot.registry.blocksByStateId[bedBlock.stateId].minStateId
       const bitMetadata = state.toString(2).padStart(4, '0') // FACING (first 2 bits), PART (3rd bit), OCCUPIED (4th bit)
diff --git a/node_modules/mineflayer/lib/plugins/entities.js b/node_modules/mineflayer/lib/plugins/entities.js
index 507a618..08cbaf5 100644
--- a/node_modules/mineflayer/lib/plugins/entities.js
+++ b/node_modules/mineflayer/lib/plugins/entities.js
@@ -271,7 +271,13 @@ function inject (bot) {
     entity.yaw = conv.fromNotchianYawByte(packet.yaw)
     entity.pitch = conv.fromNotchianPitchByte(packet.pitch)
     entity.headPitch = conv.fromNotchianPitchByte(packet.headPitch)
-    const notchVel = new Vec3(packet.velocity.x, packet.velocity.y, packet.velocity.z)
+
+    let notchVel
+    if (bot.supportFeature('entityVelocityIsLpVec3')) {
+      notchVel = new Vec3(packet.velocity.x, packet.velocity.y, packet.velocity.z)
+    } else {
+      notchVel = new Vec3(packet.velocityX, packet.velocityY, packet.velocityZ)
+    }
     entity.velocity.update(conv.fromNotchVelocity(notchVel))
     entity.metadata = parseMetadata(packet.metadata, entity.metadata)
 
@@ -281,7 +287,12 @@ function inject (bot) {
   bot._client.on('entity_velocity', (packet) => {
     // entity velocity
     const entity = fetchEntity(packet.entityId)
-    const notchVel = new Vec3(packet.velocity.x, packet.velocity.y, packet.velocity.z)
+    let notchVel
+    if (bot.supportFeature('entityVelocityIsLpVec3')) {
+      notchVel = new Vec3(packet.velocity.x, packet.velocity.y, packet.velocity.z)
+    } else {
+      notchVel = new Vec3(packet.velocityX, packet.velocityY, packet.velocityZ)
+    }
     entity.velocity.update(conv.fromNotchVelocity(notchVel))
   })
 
@@ -349,8 +360,8 @@ function inject (bot) {
     const entity = fetchEntity(packet.entityId)
     entity.position.set(packet.x, packet.y, packet.z)
     entity.velocity.set(packet.dx, packet.dy, packet.dz)
-    entity.yaw = conv.fromNotchianYaw(packet.yaw)
-    entity.pitch = conv.fromNotchianPitch(packet.pitch)
+    entity.yaw = packet.yaw
+    entity.pitch = packet.pitch
     bot.emit('entityMoved', entity)
   })
 
@@ -411,12 +422,15 @@ function inject (bot) {
       bot.emit('entityElytraFlew', entity)
     }
   }
+  bot._setElytraFlyingState = setElytraFlyingState
 
   const knownFireworks = new Set()
   function handleBotUsedFireworkRocket (fireworkEntityId, fireworkInfo) {
     if (knownFireworks.has(fireworkEntityId)) return
     knownFireworks.add(fireworkEntityId)
-    let flightDur = fireworkInfo?.nbtData?.value?.Fireworks?.value?.Flight.value ?? 1
+    let flightDur = fireworkInfo?.nbtData?.value?.Fireworks?.value?.Flight.value ??
+      fireworkInfo?.nbt?.value?.Fireworks?.value?.Flight.value ??
+      1
     if (typeof flightDur !== 'number') { flightDur = 1 }
     const baseDuration = 10 * (flightDur + 1)
     const randomDuration = Math.floor(Math.random() * 6) + Math.floor(Math.random() * 7)
@@ -424,6 +438,7 @@ function inject (bot) {
 
     bot.emit('usedFirework', fireworkEntityId)
   }
+  bot._handleFireworkRocketUse = handleBotUsedFireworkRocket
 
   let fireworkEntityName
   if (bot.supportFeature('fireworkNamePlural')) {
@@ -900,22 +915,20 @@ function inject (bot) {
 
   function useEntity (target, leftClick, x, y, z) {
     const sneaking = bot.getControlState('sneak')
-    if (x && y && z) {
-      bot._client.write('use_entity', {
-        target: target.id,
-        mouse: leftClick,
-        x,
-        y,
-        z,
-        sneaking
-      })
-    } else {
-      bot._client.write('use_entity', {
-        target: target.id,
-        mouse: leftClick,
-        sneaking
-      })
-    }
+    // 26.1（本项目补丁）：useEntityUsesEntityId 特性不存在（恒 false）——旧门控分支
+    // 是死代码；attackUsesOwnPacket=true → 独立 attack 包（正式攻击通道）；
+    // 其余统一新式 use_entity {target, hand, location(lpVec3 必填), sneaking}
+    if (leftClick === 1 && bot.supportFeature('attackUsesOwnPacket')) {
+      bot._client.write('attack', { entityId: target.id })
+      return
+    }
+
+    bot._client.write('use_entity', {
+      target: target.id,
+      hand: 0,
+      location: { x: x ?? 0, y: y ?? 0, z: z ?? 0 },
+      sneaking
+    })
   }
 
   function fetchEntity (id) {
diff --git a/node_modules/mineflayer/lib/plugins/inventory.js b/node_modules/mineflayer/lib/plugins/inventory.js
index 0636faa..8c6224f 100644
--- a/node_modules/mineflayer/lib/plugins/inventory.js
+++ b/node_modules/mineflayer/lib/plugins/inventory.js
@@ -115,6 +115,7 @@ function inject (bot, { hideErrors }) {
   function activateItem (offHand = false) {
     bot.usingHeldItem = true
     sequence++
+    const heldItem = offHand ? bot.inventory.slots[45] : bot.heldItem
 
     if (bot.supportFeature('useItemWithBlockPlace')) {
       bot._client.write('block_place', {
@@ -135,6 +136,9 @@ function inject (bot, { hideErrors }) {
         }
       })
     }
+    if (heldItem?.name === 'firework_rocket' && bot.entity?.elytraFlying) {
+      bot._handleFireworkRocketUse?.(`local:${sequence}`, heldItem)
+    }
   }
 
   function deactivateItem () {
@@ -243,25 +247,29 @@ function inject (bot, { hideErrors }) {
   async function activateEntity (entity) {
     // TODO: tell the server that we are not sneaking while doing this
     await bot.lookAt(entity.position.offset(0, 1, 0), false)
+    // 26.1（本项目补丁）：useEntityUsesEntityId 死代码门控移除——统一新式
+    // use_entity {target, hand, location(lpVec3 必填), sneaking}
     bot._client.write('use_entity', {
       target: entity.id,
-      mouse: 0, // interact with entity
-      sneaking: false,
-      hand: 0 // interact with the main hand
+      hand: 0,
+      location: { x: 0, y: 0, z: 0 },
+      sneaking: false
     })
   }
 
   async function activateEntityAt (entity, position) {
     // TODO: tell the server that we are not sneaking while doing this
     await bot.lookAt(position, false)
+    // 同 activateEntity：新式 use_entity（location 为相对偏移——lpVec3 必填）
     bot._client.write('use_entity', {
       target: entity.id,
-      mouse: 2, // interact with entity at
-      sneaking: false,
-      hand: 0, // interact with the main hand
-      x: position.x - entity.position.x,
-      y: position.y - entity.position.y,
-      z: position.z - entity.position.z
+      hand: 0,
+      location: {
+        x: position.x - entity.position.x,
+        y: position.y - entity.position.y,
+        z: position.z - entity.position.z
+      },
+      sneaking: false
     })
   }
 
diff --git a/node_modules/mineflayer/lib/plugins/physics.js b/node_modules/mineflayer/lib/plugins/physics.js
index cecae69..eedc712 100644
--- a/node_modules/mineflayer/lib/plugins/physics.js
+++ b/node_modules/mineflayer/lib/plugins/physics.js
@@ -83,6 +83,12 @@ function inject (bot, { physicsEnabled, maxCatchupTicks }) {
       bot.emit('physicsTick')
       bot.emit('physicTick') // Deprecated, only exists to support old plugins. May be removed in the future
     }
+    if (bot.entity.elytraFlying && bot.entity.onGround) {
+      bot._setElytraFlyingState?.(bot.entity, false)
+    }
+    if (bot.fireworkRocketDuration > 0) {
+      bot.fireworkRocketDuration--
+    }
     if (shouldUsePhysics) {
       updatePosition(now)
     }
@@ -240,6 +246,7 @@ function inject (bot, { physicsEnabled, maxCatchupTicks }) {
       actionId: bot.supportFeature('entityActionUsesStringMapper') ? 'start_elytra_flying' : 8,
       jumpBoost: 0
     })
+    bot._setElytraFlyingState?.(bot.entity, true)
   }
 
   bot.setControlState = (control, state) => {
diff --git a/node_modules/mineflayer/lib/plugins/time.js b/node_modules/mineflayer/lib/plugins/time.js
index 0b9411d..df8f463 100644
--- a/node_modules/mineflayer/lib/plugins/time.js
+++ b/node_modules/mineflayer/lib/plugins/time.js
@@ -13,9 +13,40 @@ function inject (bot) {
     age: null
   }
   bot._client.on('update_time', (packet) => {
-    const time = longToBigInt(packet.time)
-    const age = longToBigInt(packet.age)
-    const doDaylightCycle = packet.tickDayTime !== undefined ? !!packet.tickDayTime : time >= 0n
+    // 26.1：clockUpdates 反序列化为对象数组 [{id,totalTicks,partialTick,rate}]——
+    // 旧 readClockUpdates 按原始 Buffer 逐字节解析恒 null → 回退 age 当 dayTime
+    const clockUpdate = Array.isArray(packet.clockUpdates)
+      ? packet.clockUpdates.find(c => c.id === 0) ?? null
+      : null
+    const packetAge = packet.age ?? packet.gameTime ?? packet.time
+    if (packetAge == null) return
+
+    const age = longToBigInt(packetAge)
+    const previousAge = bot.time.bigAge
+    const previousTime = bot.time.bigTime
+
+    let packetTime = packet.time ?? clockUpdate?.totalTicks
+    const daylightBasis = packetTime ?? packet.gameTime ?? packet.age
+    const packetDayTime = packetTime == null ? null : longToBigInt(packetTime)
+    let doDaylightCycle
+    if (packet.tickDayTime !== undefined) {
+      doDaylightCycle = !!packet.tickDayTime
+    } else if (clockUpdate?.rate !== undefined) {
+      doDaylightCycle = clockUpdate.rate > 0
+    } else if (packetDayTime != null) {
+      doDaylightCycle = packetDayTime >= 0n
+    } else {
+      doDaylightCycle = bot.time.doDaylightCycle ?? (longToBigInt(daylightBasis) >= 0n)
+    }
+
+    if (packetTime == null && previousTime != null && previousAge != null) {
+      const ageDelta = age - previousAge
+      packetTime = doDaylightCycle ? previousTime + ageDelta : previousTime
+    }
+    packetTime ??= packet.gameTime ?? packet.age
+    if (packetTime == null) return
+
+    const time = packetDayTime ?? longToBigInt(packetTime)
     // When doDaylightCycle is false, we need to take the absolute value of time
     const finalTime = doDaylightCycle ? time : (time < 0n ? -time : time)
 
@@ -34,5 +65,7 @@ function inject (bot) {
 }
 
 function longToBigInt (arr) {
+  if (typeof arr === 'bigint') return BigInt.asIntN(64, arr)
+  if (typeof arr === 'number') return BigInt.asIntN(64, BigInt(arr))
   return BigInt.asIntN(64, (BigInt(arr[0]) << 32n)) | BigInt(arr[1])
 }
diff --git a/node_modules/mineflayer/lib/version.js b/node_modules/mineflayer/lib/version.js
index c4843bb..d96f9f8 100644
--- a/node_modules/mineflayer/lib/version.js
+++ b/node_modules/mineflayer/lib/version.js
@@ -1,4 +1,4 @@
-const testedVersions = ['1.8.8', '1.9.4', '1.10.2', '1.11.2', '1.12.2', '1.13.2', '1.14.4', '1.15.2', '1.16.5', '1.17.1', '1.18.2', '1.19', '1.19.2', '1.19.3', '1.19.4', '1.20.1', '1.20.2', '1.20.4', '1.20.6', '1.21.1', '1.21.3', '1.21.4', '1.21.5', '1.21.6', '1.21.8', '1.21.9', '1.21.11']
+const testedVersions = ['1.8.8', '1.9.4', '1.10.2', '1.11.2', '1.12.2', '1.13.2', '1.14.4', '1.15.2', '1.16.5', '1.17.1', '1.18.2', '1.19', '1.19.2', '1.19.3', '1.19.4', '1.20.1', '1.20.2', '1.20.4', '1.20.6', '1.21.1', '1.21.3', '1.21.4', '1.21.5', '1.21.6', '1.21.8', '1.21.9', '1.21.11', '26.1.2']
 module.exports = {
 
   testedVersions,
diff --git a/node_modules/mineflayer/lib/plugins/chat.js b/node_modules/mineflayer/lib/plugins/chat.js
index 8f9e3c2..a1b2c3d 100644
--- a/node_modules/mineflayer/lib/plugins/chat.js
+++ b/node_modules/mineflayer/lib/plugins/chat.js
@@ -132,7 +132,9 @@ function inject (bot, options) {
   })

   bot._client.on('systemChat', (data) => {
-    const msg = ChatMessage.fromNotch(data.formattedMessage)
+    // 26.1 协议：systemChat 包 content 是 anonymousNbt（旧版 formattedMessage 已移除）——
+    // fromNotch 支持 chatPacketsUseNbtComponents（content 对象有 type 字段走 NBT 分支）
+    const msg = ChatMessage.fromNotch(data.formattedMessage ?? data.content)
     const chatPositions = {
       1: 'system',
       2: 'game_info'
diff --git a/node_modules/mineflayer/lib/plugins/rain.js b/node_modules/mineflayer/lib/plugins/rain.js
index c38c286..8247b3c 100644
--- a/node_modules/mineflayer/lib/plugins/rain.js
+++ b/node_modules/mineflayer/lib/plugins/rain.js
@@ -14,10 +14,14 @@ function inject (bot) {
       bot.isRaining = false
       bot.emit('rain')
     } else if (reason === 'rain_level_change') {
+      // 26.1 实测：服务器只用 rain_level_change 同步雨量（0-1 渐变），不发 start/stop——
+      // isRaining 由 level 驱动（level > 0 = 正在下雨），否则卡在旧值输出相反天气
       bot.rainState = packet.gameMode
+      bot.isRaining = packet.gameMode > 0
       bot.emit('weatherUpdate')
     } else if (reason === 'thunder_level_change') {
       bot.thunderState = packet.gameMode
+      bot.isThundering = packet.gameMode > 0
       bot.emit('weatherUpdate')
     }
   })
