# mineflayer 与 minecraft-protocol 使用 git 引用（PR 分支，支持 MC 26.1.2 协议 775；
# prismarine-chunk/physics 已随 1.41.0/1.11.1 官方发布，2026-07-31 起不再 pin）。
# npm arborist 在 peer 校验阶段无法解析 git 依赖的版本（显示 undefined），
# 但语义上 git 版 mineflayer 4.37.0 满足各插件的 peer 范围（^4.10.0 等）。
# legacy-peer-deps 跳过自动 peer 解析，配合 package.json overrides 确保单一 mineflayer 副本。
legacy-peer-deps=true
# npm 12+ 默认禁止 git 依赖（供应链安全），本项目依赖 PR 分支 pin，需显式放行。
# 全部 git 引用均为官方 PrismarineJS 仓库的完整 SHA 固定（不可变），供应链风险已被 pin 消除
allow-git=all
