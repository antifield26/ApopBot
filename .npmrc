# v1.0.0（C1）：全部依赖切回官方 npm 版（mineflayer ^4.37.1 / minecraft-protocol ^1.66.2 /
# minecraft-data ^3.113.0），26.1.2 协议 775 适配由 patches/ 内 patch-package 补丁承担
# （postinstall 自动应用）。不再有 git 依赖 → 不再需要 allow-git 放行。
# legacy-peer-deps：npm arborist 对 mineflayer 官方版的 peer 校验在 26.1.2 补丁后仍
# 无法解析（补丁改动不影响版本号），保留跳过自动 peer 解析以维持单一副本解析
legacy-peer-deps=true
